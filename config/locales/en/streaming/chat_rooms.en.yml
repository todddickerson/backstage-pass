en:
  streaming/chat_rooms: &chat_rooms
    label: &label Chat Rooms
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Chat Room
      create: Create Chat Room
      edit: Edit Chat Room
      update: Update Chat Room
      destroy: Remove Chat Room
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{chat_room_name}? This will also remove any child resources and can't be undone.
    chat_room:
      buttons: *buttons
    fields: &fields
      id:
        heading: Chat Room ID
      stream_id:
        heading: Stream ID
      stream_id:
        _: &stream_id Stream ID
        label: *stream_id
        heading: *stream_id
        api_title: *stream_id
        api_description: *stream_id
      channel_id:
        _: &channel_id Channel ID
        label: *channel_id
        heading: *channel_id
        api_title: *channel_id
        api_description: *channel_id
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Chat Rooms"
      index: "List Chat Rooms"
      create: "Add a New Chat Room"
      member_actions: "Actions for an Individual Chat Room"
      show: "Retrieve a Chat Room"
      update: "Update a Chat Room"
      destroy: "Delete a Chat Room"
      fields: *fields
    index:
      section: "%{streams_possessive} Chat Rooms"
      contexts:
        stream:
          header: Chat Rooms
          description: Below is a list of Chat Rooms that have been added for %{stream_name}.
          description_empty: No Chat Rooms have been added for %{stream_name}.
      fields: *fields
      buttons: *buttons
    show:
      section: "%{chat_room_name}"
      header: Chat Room Details
      description: Below are the details we have for %{chat_room_name}.
      manage_description: You'll also find options for updating these details or removing %{chat_room_name} from %{stream_name} entirely.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "New Chat Room for %{stream_name}"
      header: New Chat Room Details
      description: Please provide the details of the new Chat Room you'd like to add to %{stream_name}.
      form: *form
    edit:
      section: "%{chat_room_name}"
      header: Edit Chat Room Details
      description: You can update the details or settings for %{chat_room_name} below.
      form: *form
    notifications:
      created: Chat Room was successfully created.
      updated: Chat Room was successfully updated.
      destroyed: Chat Room was successfully destroyed.
  account:
    streaming:
      chat_rooms: *chat_rooms
  activerecord:
    attributes:
      streaming/chat_room:
        stream_id: *stream_id
        channel_id: *channel_id
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
