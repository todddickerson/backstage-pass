json.extract! waitlist_entry,
  :id,
  :access_pass_id,
  :email,
  :first_name,
  :last_name,
  :answers,
  :status,
  :notes,
  :approved_at,
  :rejected_at,
  # 🚅 super scaffolding will insert new fields above this line.
  :created_at,
  :updated_at

# 🚅 super scaffolding will insert file-related logic above this line.
