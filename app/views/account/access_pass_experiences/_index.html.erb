<% access_pass_experiences = access_pass_experiences.accessible_by(current_ability) %>
<% access_pass = @access_pass %>
<% context ||= access_pass %>
<% collection ||= :access_pass_experiences %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% pagy ||= nil %>
<% pagy, access_pass_experiences = pagy(access_pass_experiences, page_param: :access_pass_experiences_page) unless pagy %>

<%= action_model_select_controller do %>
  <% cable_ready_updates_for context, collection do %>
    <%= render 'account/shared/box', pagy: pagy do |box| %>
      <% box.title t(".contexts.#{context.class.name.underscore}.header") %>
      <% box.description do %>
        <%= t(".contexts.#{context.class.name.underscore}.description#{"_empty" unless access_pass_experiences.any?}") %>
        <%= render "shared/limits/index", model: access_pass_experiences.model %>
      <% end %>

      <% box.table do %>
        <% if access_pass_experiences.any? %>
          <table class="table">
            <thead>
              <tr>
                <%= render "shared/tables/select_all" %>
                <th><%= t('.fields.experience.heading') %></th>
                <th  class="text-center"><%= t('.fields.included.heading') %></th>
                <th><%= t('.fields.position.heading') %></th>
                <%# 🚅 super scaffolding will insert new field headers above this line. %>
                <th><%= t('.fields.created_at.heading') %></th>
                <th class="text-right"></th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: 'account/access_pass_experiences/access_pass_experience', collection: access_pass_experiences %>
            </tbody>
          </table>
        <% end %>
      <% end %>

      <% box.actions do %>
        <% unless hide_actions %>
          <% if context == access_pass %>
            <% if can? :create, AccessPassExperience.new(access_pass: access_pass) %>
              <%= link_to t('.buttons.new'), [:new, :account, access_pass, :access_pass_experience], class: "#{first_button_primary(:access_pass_experience)} new" %>
            <% end %>
          <% end %>

          <%# 🚅 super scaffolding will insert new targets one parent action model buttons above this line. %>
          <%# 🚅 super scaffolding will insert new bulk action model buttons above this line. %>
          <%= render "shared/bulk_action_select" if access_pass_experiences.any? %>

          <% unless hide_back %>
            <%= link_to t('global.buttons.back'), [:account, context], class: "#{first_button_primary(:access_pass_experience)} back" %>
          <% end %>
        <% end %>
      <% end %>

      <% box.raw_footer do %>
        <%# 🚅 super scaffolding will insert new action model index views above this line. %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
