<% cancel_path ||= experience.persisted? ? [:account, experience] : [:account, @space, :experiences] %>

<%= form_with model: experience, url: (experience.persisted? ? [:account, experience] : [:account, @space, :experiences]), class: 'form' do |form| %>
  <%= render "shared/limits/form", form: form, cancel_path: cancel_path do %>
    <%= render 'account/shared/forms/errors', form: form %>

    <% with_field_settings form: form do %>
      <%= render 'shared/fields/text_field', method: :name, options: {autofocus: true} %>
      <%= render 'shared/fields/trix_editor', method: :description %>
      <%= render 'shared/fields/buttons', method: :experience_type %>
      <%= render 'shared/fields/number_field', method: :price_cents %>
      <%# 🚅 super scaffolding will insert new fields above this line. %>
    <% end %>

    <div class="buttons">
      <%= form.submit (form.object.persisted? ? t('.buttons.update') : t('.buttons.create')), class: "button" %>
      <%= link_to t('global.buttons.cancel'), cancel_path, class: "button-secondary" %>
    </div>
  <% end %>
<% end %>
