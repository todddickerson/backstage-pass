<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= cable_ready_updates_for @purchase do %>
      <%= render 'account/shared/box', divider: true do |box| %>
        <% box.title t('.header') %>
        <% box.description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @purchase %>
        <% end %>

        <% box.body do %>
          <% with_attribute_settings object: @purchase, strategy: :label do %>
            <%= render 'shared/attributes/belongs_to', attribute: :user %>
            <%= render 'shared/attributes/belongs_to', attribute: :access_pass %>
            <%= render 'shared/attributes/number', attribute: :amount_cents %>
            <%= render 'shared/attributes/text', attribute: :stripe_charge_id %>
            <%= render 'shared/attributes/text', attribute: :stripe_payment_intent_id %>
            <%= render 'shared/attributes/option', attribute: :status %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% box.actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @purchase], class: first_button_primary if can? :edit, @purchase %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @purchase], method: :delete, class: first_button_primary, data: { turbo_confirm: t('.buttons.confirmations.destroy', model_locales(@purchase)) } if can? :destroy, @purchase %>
          <%= link_to t('global.buttons.back'), [:account, @team, :billing_purchases], class: first_button_primary %>
        <% end %>

        <% box.raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
